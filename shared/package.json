{
  "name": "@menuboard/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared types, enums, permission matrix and constants for MenuBoard",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "typecheck": "tsc -b --noEmit false --dry 2>nul || tsc -b",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.1.9"
  }
}